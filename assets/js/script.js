const startButton = document.getElementById('startButton');
const outputText = document.getElementById('outputText');

startButton.addEventListener('click', () => {
    outputText.textContent = 'Thanks for clicking! c:';
});